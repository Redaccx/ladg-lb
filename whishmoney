<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Coming Soon</title>
<style>
  * { margin:0; padding:0; box-sizing:border-box; font-family:'Arial', sans-serif; }

  body {
    height:100vh;
    display:flex;
    justify-content:center;
    align-items:center;
    background: linear-gradient(135deg, #C1272D, #8e1a21);
    color:#fff;
    overflow:hidden;
  }

  .container {
    text-align:center;
    position: relative;
    max-width:700px;
  }

  h1 {
    font-size:3rem;
    margin-bottom:20px;
    letter-spacing:2px;
    animation: slideDown 1s ease forwards;
  }

  p {
    font-size:1.2rem;
    margin-bottom:40px;
    animation: fadeIn 2s ease forwards;
  }

  /* Card Animation */
  .card {
    width: 320px;
    height: 200px;
    background: linear-gradient(145deg, #fff, #eee);
    border-radius: 20px;
    color: #333;
    margin:0 auto;
    padding:20px;
    box-shadow: 0 15px 30px rgba(0,0,0,0.2);
    perspective: 1000px;
    transform-style: preserve-3d;
    animation: float 3s ease-in-out infinite;
    position: relative;
  }

  .card-front {
    width:100%;
    height:100%;
    border-radius:20px;
    background: linear-gradient(135deg, #C1272D, #8e1a21);
    color:#fff;
    padding:20px;
    position:absolute;
    backface-visibility: hidden;
  }

  .card-front h2 {
    margin-bottom:50px;
    font-size:1.5rem;
  }

  .card-front .chip {
    width:50px;
    height:35px;
    background: gold;
    border-radius:5px;
    margin-bottom:30px;
  }

  .card-front .number {
    letter-spacing: 3px;
    font-size: 1.2rem;
    margin-bottom:20px;
  }

  .card-front .name {
    text-transform: uppercase;
    font-size: 0.9rem;
    display:flex;
    justify-content: space-between;
  }

  /* Animations */
  @keyframes slideDown {
    0% { opacity:0; transform: translateY(-50px);}
    100% { opacity:1; transform: translateY(0);}
  }

  @keyframes fadeIn {
    0% {opacity:0;}
    100% {opacity:1;}
  }

  @keyframes float {
    0%, 100% { transform: translateY(0) rotateX(0) rotateY(0);}
    50% { transform: translateY(-20px) rotateX(5deg) rotateY(5deg);}
  }

  /* Floating shapes in background */
  .shape {
    position: absolute;
    border-radius: 50%;
    opacity:0.2;
    animation: moveShape 10s linear infinite;
  }

  .shape1 { width:80px; height:80px; background:#fff; top:10%; left:5%; animation-delay:0s; }
  .shape2 { width:120px; height:120px; background:#ffdcdc; top:70%; left:80%; animation-delay:2s; }
  .shape3 { width:50px; height:50px; background:#fff; top:40%; left:60%; animation-delay:4s; }

  @keyframes moveShape {
    0% { transform: translateY(0) rotate(0deg); }
    50% { transform: translateY(-100px) rotate(180deg); }
    100% { transform: translateY(0) rotate(360deg); }
  }

  /* Responsive */
  @media(max-width:500px){
    h1 { font-size:2rem; }
    .card { width:260px; height:160px; }
  }
</style>
</head>
<body>

<div class="container">
  <h1>Coming Soon</h1>
  <p>طريقة التبرع عن طريق <strong>لويش</strong> ستكون متاحة قريبًا!</p>

  <div class="card">
    <div class="card-front">
      <div class="chip"></div>
      <h2>VISA</h2>
      <div class="number">1234 5678 9012 3456</div>
      <div class="name">
        <span>hasan abdallah</span>
        <span>12/28</span>
      </div>
    </div>
  </div>
</div>

<div class="shape shape1"></div>
<div class="shape shape2"></div>
<div class="shape shape3"></div>

</body>
</html>
